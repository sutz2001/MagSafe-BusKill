name: Release Please

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
    # Only run on files that release-please modifies
    paths:
      - ".github/.release-please-manifest.json"
      - "**/CHANGELOG.md"
      - "**/package.json"
      - "**/package-lock.json"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  release-please:
    name: "Release Please"
    runs-on: ubuntu-latest
    outputs:
      pr: ${{ steps.release.outputs.pr }}
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Release Please
        id: release
        uses: googleapis/release-please-action@0b6b3fc0186a2f7118bfd88fab9ea481e1839504 # v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          config-file: .github/.release-please-config.json
          manifest-file: .github/.release-please-manifest.json

  auto-approve:
    name: Auto Approve
    needs: release-please
    runs-on: ubuntu-latest
    if: startsWith(github.head_ref, 'release-please')
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Auto-approve Release PR
        uses: lekman/auto-approve-action@0d66995267956b0f0c558ca0a7528df79cb069f2 # v1
        with:
          allowed-authors: "app/lekman-release-please-bot"
          github-token: ${{ secrets.APPROVE_TOKEN }}
          required-labels: "autorelease: pending"
          label-match-mode: "all"
          path-filters: ".github/.release-please-manifest.json,**/CHANGELOG.md,**/package.json,**/package-lock.json"
          merge-method: "merge"

  merge-on-approval:
    name: Merge
    needs: [release-please, auto-approve]
    runs-on: ubuntu-latest
    if: ${{ needs.release-please.outputs.pr }}
    steps:
      - name: Generate GitHub App Token
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      - name: Enable Auto-merge
        run: |
          gh pr merge ${{ fromJson(needs.release-please.outputs.pr).number }} \
            --auto \
            --merge \
            --delete-branch
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          GH_REPO: ${{ github.repository }}
