# SwiftLint Configuration for MagSafe Guard
# Focus on documentation, code quality, and maintainability

# Files to include/exclude
included:
  - MagSafeGuard
  - MagSafeGuardTests
  - MagSafeGuardUITests
only_rules: []

# Analyzer rules (requires 'swiftlint analyze')
analyzer_rules:
  - unused_import # Check for unused imports

# Opt-in rules (not enabled by default)
opt_in_rules:
  - file_header # Consistent file headers
  - explicit_type_interface # Explicit types for public APIs
  - explicit_acl # Explicit access control
  - explicit_init # Explicit initializers
  - redundant_type_annotation # Remove redundant type annotations
  - sorted_imports # Sort import statements
  - vertical_parameter_alignment_on_call
  - closure_parameter_position
  - multiline_function_chains
  - multiline_parameters
  - operator_usage_whitespace
  - redundant_nil_coalescing
  - toggle_bool
  - yoda_condition

# Disabled rules
disabled_rules:
  - todo # Allow TODO comments during development
  - missing_docs # Public API docs enforced selectively, not in quick QA
  - function_body_length # Complex UI code can be longer
  - explicit_type_interface # Too strict for internal properties
  - file_length # Some files need to be longer temporarily
  - explicit_acl # Extension methods don't need explicit ACL
  - large_tuple # Notification tuples need 3 elements
  - line_length # Long lines are sometimes necessary for readability
  - type_body_length # Some service classes are necessarily large

# Rule configurations
file_header:
  required_pattern: |
    \/\/
    \/\/  .*\.swift
    \/\/  MagSafe Guard
    \/\/

# Line lengths - commented out since rule is disabled
# line_length:
#   warning: 120
#   error: 200
#   ignores_comments: true
#   ignores_urls: true

# Function length (generous for SwiftUI views) - commented out since disabled
# function_body_length:
#   warning: 60
#   error: 100

# Type body length - commented out since disabled
# type_body_length:
#   warning: 310 # Increased slightly to accommodate test files
#   error: 400

# File length - commented out since disabled
# file_length:
#   warning: 500
#   error: 800

# Cyclomatic complexity
cyclomatic_complexity:
  warning: 10
  error: 15

# Nesting depth
nesting:
  type_level: 2
  function_level: 5 # SwiftUI can have deep nesting

# Function parameter count
function_parameter_count:
  warning: 6
  error: 8

# Identifier names
identifier_name:
  min_length: 2
  max_length: 50
  excluded:
    - id
    - x
    - y

# Type name
type_name:
  min_length: 3
  max_length: 50

# Custom rules for this project
custom_rules:
  no_print_statements:
    name: "No Print Statements"
    regex: 'print\('
    message: "Use proper logging instead of print statements"
    severity: warning

  security_todo:
    name: "Security TODO"
    regex: '//[^\n]*TODO[^\n]*[Ss]ecurity'
    message: "Security-related TODOs should be prioritized"
    severity: warning # Changed from error to warning

# Reporter type
reporter: "xcode"

# Exclude auto-generated code
excluded:
  - .build
  - DerivedData
  - "*.generated.swift"
  - "**/Generated/"
