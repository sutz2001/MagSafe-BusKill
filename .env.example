# Local developer secrets (copy to .env — never commit .env)

# GitHub CLI / API (optional, for gh and release tasks)
GITHUB_TOKEN=
GITHUB_USERNAME=

# Sentry (optional — app reads SENTRY_* at runtime when enabled)
SENTRY_DSN=
SENTRY_ENABLED=false
SENTRY_ENVIRONMENT=development
SENTRY_DEBUG=false
